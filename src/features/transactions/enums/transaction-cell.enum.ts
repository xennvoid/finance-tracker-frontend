export enum TransactionCellKey {
  DESCRIPTION = 'description',
  TYPE = 'type',
  NUMBER = 'number',
  DATE = 'date',
  AMOUNT = 'amount',
  CURRENCY = 'currency',
}

export const TransactionCellKeys = Object.values(TransactionCellKey);
